# CHZZK 쿠폰 폴더 설정 가이드

CHZZK(치지직) 자동화에서 "쿠폰 상위 폴더 경로"를 설정해두면, 캠페인을 만들 때마다 쿠폰 엑셀 파일을 자동으로 찾아서 리워드에 첨부해줍니다. 이 문서는 그 폴더를 어떻게 구성해야 하는지 정리한 가이드입니다.

## 전체 구조 한눈에 보기

```
로드 투 마스터즈/                 ← ① "쿠폰 상위 폴더 경로"에 입력하는 폴더
├── 0903/                         ← ② 캠페인 시작일(MMDD)이 포함된 하위 폴더
│   ├── 치지직_1.xlsx             ← ③ 1번째 리워드 티어용 쿠폰 파일
│   ├── 치지직_2.xlsx             ← ③ 2번째 리워드 티어용 쿠폰 파일
│   └── 치지직_3.xlsx             ← ③ 3번째 리워드 티어용 쿠폰 파일
├── 0904/
│   ├── 치지직_1.xlsx
│   ├── 치지직_2.xlsx
│   └── 치지직_3.xlsx
└── 0905/
    ├── 치지직_1.xlsx
    ├── 치지직_2.xlsx
    └── 치지직_3.xlsx
```

## ① 쿠폰 상위 폴더 경로

- 화면의 **"쿠폰 파일 상위 폴더 경로"** 칸에 입력하는 폴더입니다.
- 이 폴더 자체에는 쿠폰 파일을 직접 넣지 않고, **날짜별 하위 폴더**들을 그 안에 만들어둡니다.
- 예: `C:\Users\Nimble_User\Desktop\드롭스\로드 투 마스터즈`

## ② 날짜별 하위 폴더 이름 (가장 중요)

- 각 캠페인의 **시작일(시작 KST 칸에 입력한 날짜)**을 `MMDD` 4자리로 변환한 값이 **폴더 이름에 포함**되어 있어야 합니다.
  - 예: 시작일이 `2026-09-03 05:00`이면 → `0903`이 폴더 이름에 포함되어야 함
- **완전히 똑같을 필요는 없고, 그 문자열이 폴더 이름 안에 들어있기만 하면** 찾습니다. 그래서 아래 이름들은 전부 정상 작동합니다.
  - `0903`
  - `9월3일`
  - `로드투마스터즈_0903_최종`
- ⚠️ 대신 **같은 상위 폴더 안에 `0903`이 포함된 폴더가 2개 이상 있으면 안 됩니다.** 어떤 걸 써야 할지 판단할 수 없어서 오류가 납니다.
- ⚠️ 날짜는 **시작일 기준**입니다. 종료일이나 리워드 유효기간이 아닙니다.

## ③ 쿠폰 파일 이름

- 하위 폴더 안의 파일 이름은 **정확히** 아래 형식이어야 합니다 (오타나 띄어쓰기 차이도 안 됩니다).
  ```
  치지직_1.xlsx
  치지직_2.xlsx
  치지직_3.xlsx
  ...
  ```
- 숫자는 **화면의 "리워드 티어 구성" 표에서 위에서부터 몇 번째 줄인지**와 그대로 매칭됩니다.
  - 리워드 티어를 3개 설정했다면 `치지직_1.xlsx` ~ `치지직_3.xlsx` 까지 **3개 모두** 있어야 합니다.
  - 하나라도 없으면 그 시점에서 오류가 나고 해당 캠페인 생성이 중단됩니다.
- 파일 형식은 `.xlsx` (엑셀)만 인식합니다.

## 자주 하는 실수

| 증상 | 원인 |
| --- | --- |
| "쿠폰 상위 폴더를 찾을 수 없습니다" | 입력한 경로 자체가 틀렸거나 오타가 있음 |
| `"0903"가 포함된 하위 폴더를 찾을 수 없습니다` | 그 날짜의 하위 폴더를 아직 안 만들었거나, 폴더명에 MMDD가 안 들어있음 |
| 쿠폰 파일을 찾을 수 없습니다 (`치지직_2.xlsx`) | 리워드 티어 개수만큼 파일을 다 안 넣었거나, 파일명이 정확히 안 맞음 (띄어쓰기, 밑줄, 확장자 등) |
| 엉뚱한 날짜 폴더의 쿠폰이 들어감 | 폴더명에 여러 날짜가 겹쳐 들어가 있어서 (예: `0903` 폴더 안에 `0903_0904`처럼) 의도치 않게 다른 날짜와도 매칭됨 |

## 체크리스트

캠페인 실행 전에 이것만 확인하면 됩니다:

- [ ] 상위 폴더 경로가 정확한가
- [ ] 이번에 만들 캠페인들의 시작일마다 하위 폴더가 하나씩 있는가 (MMDD 포함, 중복 없이)
- [ ] 각 하위 폴더 안에 리워드 티어 개수만큼 `치지직_1.xlsx`, `치지직_2.xlsx`... 파일이 다 있는가
